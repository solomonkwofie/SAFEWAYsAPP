import 'package:flutter/material.dart';

class TypesofBuses extends StatelessWidget {
  const TypesofBuses({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar:AppBar()
      
    );
  }
}